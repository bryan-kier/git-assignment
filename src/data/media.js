// Set once a real hero photo exists at frontend/public/hero/<file> — see the
// README there. Until then, the homepage hero falls back to the vector
// JerseyGraphic render.
export const HERO_IMAGE = null;
